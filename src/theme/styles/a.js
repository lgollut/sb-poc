/**
 * Theme-ui <Link /> component use that as it's default variant
 */
export const a = {
  color: 'primary',
  fontFamily: 'body',
  textDecoration: 'none',
  ':hover': {
    textDecoration: 'underline',
  },
};
