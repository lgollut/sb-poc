/**
 * Styles defined here will be applied as an emotion css-global
 * stylesheet on the body html element.
 * Hence, you can style the app root html element as follow
 *
 * '& #app-root': {
 *   minHeight: '100%',
 * }
 *
 */
export const root = {};
